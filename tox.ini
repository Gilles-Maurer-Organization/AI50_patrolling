[tox]
envlist = py39
skipsdist = True

[testenv]
deps =
    coverage
commands =
    coverage run -m unittest discover -s tests
    coverage xml
    coverage report

[coverage:run]
relative_files = True
source = services/
branch = True

[coverage:report]
show_missing = True